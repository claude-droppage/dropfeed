import PreviewView from '@/components/preview/PreviewView'

// Podgląd perełek (gem_score) — READ-ONLY, NIE rusza live'a (dni 18/19, feed).
export default function PreviewPage() {
  return <PreviewView />
}
